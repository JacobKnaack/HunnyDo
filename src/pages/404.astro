---
import BaseLayout from '../layouts/BaseLayout.astro';
import SiteHeader from '../components/SiteHeader.astro';
import Button from '../components/Button.astro';
import Footer from '../components/Footer.astro';
---

<BaseLayout title="Page not found — HunnyDo" description="The page you're looking for doesn't exist or has moved." noindex>
  <SiteHeader />

  <main>
    <div class="panel">
      <span class="fw-tag fw-tag-info">ERROR · 404</span>

      <div class="blob" aria-hidden="true">
        <svg viewBox="0 0 48 48" fill="none">
          <circle cx="21" cy="21" r="14" stroke="currentColor" stroke-width="2.4" stroke-dasharray="4 5.5" />
          <path d="M17.3 17.8c0-2.5 1.9-4.3 4.2-4.3 2.3 0 4.2 1.7 4.2 3.8 0 2.5-4.2 2.8-4.2 6.1" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" />
          <circle cx="21.5" cy="27.6" r="1.4" fill="currentColor" />
          <path d="M31 31L40 40" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" />
        </svg>
      </div>

      <h1>Page not found</h1>
      <p class="lede">The link's broken, or this page has moved. Let's get you back on track.</p>

      <div class="actions">
        <Button href="/" variant="primary">Go to homepage</Button>
        <Button href="mailto:jacobknck@gmail.com" variant="ghost">Contact support</Button>
      </div>

      <div class="suggest">
        <p>Or try one of these</p>
        <div class="links">
          <a href="/#features">Features</a>
          <a href="/#pricing">Pricing</a>
          <a href="/brand-kit.html">Branding</a>
          <a href="/fernwell.html">Design</a>
        </div>
      </div>
    </div>
  </main>

  <Footer />
</BaseLayout>

<style>
  :global(html),
  :global(body) {
    height: 100%;
  }
  :global(body) {
    display: flex;
    flex-direction: column;
  }

  main {
    flex: 1;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: var(--fw-sp-6) var(--fw-sp-5) var(--fw-sp-8);
  }

  .panel {
    max-width: 440px;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .panel :global(.fw-tag) {
    margin-bottom: var(--fw-sp-5);
    letter-spacing: 0.04em;
  }

  .blob {
    width: 112px;
    height: 112px;
    margin-bottom: var(--fw-sp-5);
    border-radius: 38% 62% 60% 40% / 48% 42% 58% 52%;
    background: linear-gradient(135deg, var(--fw-primary), #FFD988);
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: var(--fw-shadow-primary);
  }
  .blob svg {
    width: 48px;
    height: 48px;
    color: var(--fw-on-primary);
  }

  h1 {
    font-size: 30px;
    font-weight: 800;
    letter-spacing: -0.01em;
    line-height: 1.2;
  }

  .lede {
    font-size: 15px;
    color: var(--fw-text-soft);
    margin-top: var(--fw-sp-3);
    max-width: 38ch;
  }

  .actions {
    display: flex;
    gap: var(--fw-sp-3);
    flex-wrap: wrap;
    justify-content: center;
    margin-top: var(--fw-sp-6);
  }
  .actions :global(.fw-btn) {
    padding: 12px 22px;
    font-size: 15px;
  }

  .suggest {
    margin-top: var(--fw-sp-7);
    padding-top: var(--fw-sp-6);
    border-top: 1.5px solid var(--fw-bg-dim);
    width: 100%;
    max-width: 360px;
  }
  .suggest p {
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--fw-text-soft);
    font-family: var(--fw-font-mono);
    margin-bottom: var(--fw-sp-3);
  }
  .suggest .links {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }
  .suggest a {
    font-size: 14px;
    font-weight: 600;
    text-decoration: none;
    color: var(--fw-secondary);
    padding: 8px 10px;
    border-radius: var(--fw-r-sm);
    transition: background .12s ease;
  }
  .suggest a:hover {
    background: var(--fw-surface);
  }

  @media (max-width: 480px) {
    h1 {
      font-size: 26px;
    }
  }
</style>
