<header class="site-header">
  <a class="brand" href="/">
    <svg width="24" height="24" viewBox="0 0 120 120"><path d="M60,12 L101.57,36 L101.57,84 L60,108 L18.43,84 L18.43,36 Z" fill="#FFC145" stroke="#FFC145" stroke-width="14" stroke-linejoin="round"/><path d="M40,62 L54,76 L84,42" fill="none" stroke="#1F2430" stroke-width="10" stroke-linecap="round" stroke-linejoin="round"/></svg>
    <span><span class="a">Hunny</span><span class="do">Do</span></span>
  </a>
  <a class="header-link" href="mailto:jacobknck@gmail.com">Contact support</a>
</header>

<style>
  .site-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: var(--sp-5) var(--sp-6);
  }
  .brand {
    display: flex;
    align-items: center;
    gap: 10px;
    text-decoration: none;
    font-family: var(--font-display);
    font-weight: 800;
    font-size: 18px;
    color: var(--ink);
  }
  .brand svg {
    flex-shrink: 0;
  }
  .brand .a {
    color: var(--ink);
  }
  .brand .do {
    color: var(--marigold-dk);
  }
  .header-link {
    font-size: 14px;
    font-weight: 600;
    text-decoration: none;
    color: var(--ink-soft);
    transition: color .12s ease;
  }
  .header-link:hover {
    color: var(--ink);
  }
  @media (max-width: 480px) {
    .site-header {
      padding: var(--sp-4);
    }
  }
</style>
